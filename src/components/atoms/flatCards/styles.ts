import {StyleSheet} from 'react-native';

const styles = ({}) =>
  StyleSheet.create({
    headings: {
      fontSize: 20,
      fontWeight: '700',
    },
    continuer: {
      backgroundColor: 'red',
      borderWidth: 0.5,
      borderRadius: 10,
      width: '30%',
      height: 120,
      alignItems: 'center',
      justifyContent: 'center',
      elevation: 4,
      margin: 2,
    },
  });
export default styles;
